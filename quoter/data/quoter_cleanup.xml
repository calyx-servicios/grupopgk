<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Limpieza de artefactos antiguos (modelo eliminado quoter.pricelist.range.rate) -->
        <delete id="action_quoter_pricelist_range_rate" model="ir.actions.act_window"/>
        <delete id="view_quoter_pricelist_range_rate_tree" model="ir.ui.view"/>
        <delete id="view_quoter_pricelist_range_rate_form" model="ir.ui.view"/>
        <delete id="access_quoter_pricelist_range_rate_system" model="ir.model.access"/>
        <!-- Vistas / acción de ítems «Cotizador» en product.pricelist (reemplazados por productos Tarifa/h) -->
        <delete id="view_product_pricelist_form_quoter" model="ir.ui.view"/>
        <delete id="view_product_pricelist_form_quoter_advanced_rules" model="ir.ui.view"/>
        <delete id="view_product_pricelist_item_form_quoter" model="ir.ui.view"/>
    </data>
</odoo>

