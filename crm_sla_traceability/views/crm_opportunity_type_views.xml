<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="crm_opportunity_type_view_tree" model="ir.ui.view">
        <field name="name">crm.opportunity.type.tree</field>
        <field name="model">crm.opportunity.type</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="crm_opportunity_type_view_form" model="ir.ui.view">
        <field name="name">crm.opportunity.type.form</field>
        <field name="model">crm.opportunity.type</field>
        <field name="arch" type="xml">
            <form string="Opportunity Type">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="crm_opportunity_type_view_search" model="ir.ui.view">
        <field name="name">crm.opportunity.type.search</field>
        <field name="model">crm.opportunity.type</field>
        <field name="arch" type="xml">
            <search string="Opportunity Types">
                <field name="name"/>
                <filter name="filter_inactive" string="Archived"
                        domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="crm_opportunity_type_action" model="ir.actions.act_window">
        <field name="name">Opportunity Types</field>
        <field name="res_model">crm.opportunity.type</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
